%! TeX program = lualatex
\documentclass[conference]{IEEEtran}

\usepackage{newtxtext}
\usepackage{newtxmath}

\usepackage{silence}
\WarningFilter{caption}{Unknown document class}
\usepackage{xurl}
\usepackage{subcaption}
\usepackage{graphicx}
\usepackage{listings}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{microtype}
\usepackage{hyphenat}
\usepackage{algorithm,algorithmicx,algpseudocode}
\usepackage[style=ieee, backend=biber]{biblatex}

% --- Path configuration ---
\graphicspath{{../report/figures/}}
\addbibresource{../report/refs.bib}

% --- RISC-V assembly listings language ---
\lstdefinelanguage[RISC-V]{Assembler}
{
  alsoletter={.},
  alsodigit={0x},
  morekeywords=[1]{
    lb, lh, lw, lbu, lhu,
    sb, sh, sw,
    sll, slli, srl, srli, sra, srai,
    add, addi, sub, lui, auipc,
    xor, xori, or, ori, and, andi,
    slt, slti, sltu, sltiu,
    beq, bne, blt, bge, bltu, bgeu,
    j, jr, jal, jalr, ret,
    scall, break, nop
  },
  morekeywords=[2]{
    .align, .ascii, .asciiz, .byte, .data, .double, .extern,
    .float, .globl, .half, .kdata, .ktext, .set, .space, .text, .word
  },
  morekeywords=[3]{
    zero, ra, sp, gp, tp, s0, fp,
    t0, t1, t2, t3, t4, t5, t6,
    s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11,
    a0, a1, a2, a3, a4, a5, a6, a7,
    ft0, ft1, ft2, ft3, ft4, ft5, ft6, ft7,
    fs0, fs1, fs2, fs3, fs4, fs5, fs6, fs7, fs8, fs9, fs10, fs11,
    fa0, fa1, fa2, fa3, fa4, fa5, fa6, fa7
  },
  morecomment=[l]{;},
  morecomment=[l]{\#},
  morestring=[b]",
  morestring=[b]'
}

% --- Code listings style ---
\lstset{
    language=C,
    basicstyle=\footnotesize\ttfamily,
    breaklines=true,
    breakatwhitespace=true,
    postbreak=\mbox{\textcolor{red}{$\hookrightarrow$}\space},
    numbers=left,
    numberstyle=\tiny\color{gray},
    numbersep=6pt,
    showstringspaces=false,
    tabsize=2,
    columns=flexible,
    keepspaces=true,
    frame=single,
    framesep=3pt,
    rulecolor=\color{black!30},
    backgroundcolor=\color{gray!5},
    aboveskip=0.5em,
    belowskip=0.5em,
    xleftmargin=1.5em,
    xrightmargin=1em,
    captionpos=b,
}

% --- TikZ libraries ---
\usepackage{tikz}
\usetikzlibrary{shapes, positioning, decorations.pathreplacing}

% --- IEEE paper metadata ---
\title{Eliminating Timing Channels in Microkernels: Integrating Time Protection Mechanisms into FreeRTOS}

\author{
    \IEEEauthorblockN{Simon Vinding Brodersen}
    \IEEEauthorblockA{Department of Computer Science\\
    University of Copenhagen\\
    Copenhagen, Denmark\\
    rzn875@alumni.ku.dk}
}

\begin{document}
\maketitle

\input{sections/abstract}

\input{sections/introduction}
\input{sections/background}
\input{sections/related_work}
\input{sections/design}
\input{sections/implementation}
\input{sections/evaluation}
\input{sections/discussion}
\input{sections/conclusion}

\printbibliography

\end{document}
